# 📱 الرؤية والخطة التفصيلية لتطبيق "التاجر الذكي المتنقل" (Tajer Smart)

---

## 🎯 1. فلسفة التطبيق والجمهور المستهدف
- **الهدف الأساسي:** بناء تطبيق هاتف ذكي، بسيط جداً ومترابط تلقائياً، مصمم خصيصاً **للتاجر المتنقل أو صاحب التجارة العامة (تاجر عامي لا يحب التعقيد)**.
- **التحدي الرئيسي:** التاجر المتنقل يعمل أثناء التنقل وفي الأسواق، وليس لديه وقت لإدخال بيانات معقدة أو استخدام حواسيب محمول؛ يرغب في تسجيل المنتجات، الشراء، البيع، الموردين، الزبائن، والديون **في ثوانٍ معدودة وبضغطة زر واحدة**.

---

## 🛠️ 2. قرار التقنيات وتحديد لغة البرمجة (Tech Stack Decision)

### لغة البرمجة والبيئة المختارة:
نختار **TypeScript مع JavaScript (Node.js)** كبيئة أساسية، و **Next.js (React)** كإطار عمل رائد للأسباب التالية:

1. **توافق كامل مع الموبايل (PWA - Progressive Web App):**
   - يمكن تثبيت التطبيق مباشرة على هاتف التاجر (Android / iOS) كأيقونة تطبيق دون الحاجة للرف رفع في متجر التطبيقات.
   - يعمل بسرعة هائلة وشاشات تجاوبية مئة بالمئة (Responsive Mobile-First).
2. **العمل بدون إنترنت (Offline Capability):**
   - استخدام `IndexedDB` / `LocalStorage` وحوافظ المزامنة الآلية، بحيث إذا كان التاجر في سوق أو منطقة ضعيفة التغطية، يسجل البيع والشراء والديون فوراً، وعند توفر الشبكة تترابط البيانات تلقائياً.
3. **قاعدة البيانات الخلفية: Supabase (PostgreSQL)**
   - توفر سرعة استعلام فائقة، وتخزين سحابي لصور التاجر والزبائن والمنتجات، ونظام آمن لإدارة الجلسات والبيانات.
4. **التنسيق والواجهات: Vanilla CSS + Tailwind CSS (تصميم غاية في البساطة والجاذبية)**
   - كروت كلاسيكية واضحة، أزرار ضخمة تناسب اللمس السريع، ألوان إرشادية (أخضر للمكاسب/المسدد، أحمر للديون/الخطر، أصفر لتنبيه انتهاء الصلاحية).

---

## 💡 3. أهم المزايا والمجموعات الوظيفية التفاعلية

### 1️⃣ قائمة الأشخاص (الموردين والزبائن) - CRM المبسط جداً
- **صورة التاجر/الزبون:** إمكانية التقاط صورة مباشرة ب كاميرا الهاتف أو رفعها (ميزة رئيسية).
- **التواصل السريع بضغطة زر:**
  - **زر واتساب مباشر (WhatsApp 1-Click):** يفتح محادثة واتساب فورية مع رسالة مجهزة تلقائياً تحتوي على كشف الحساب أو تذكير بالدين.
  - **زر اتصال مباشر:** يفتح لوحة الاتصال برقم التاجر.
- **تصنيف سريع:** (زبون / مورد / كلاهما).
- **الرصيد التلقائي:** يظهر بجانب كل شخص فوراً: **"له X دينار"** (دين علينا للمورد) أو **"عليه X دينار"** (دين لنا على الزبون).

### 2️⃣ كارت المنتجات والمخزن الذكي (Smart Inventory)
- **إضافة فائقة السرعة:**
  - اسم المنتج.
  - سعر الشراء بالجملة (كم اشتراها).
  - سعر البيع بالتجزئة (بكم يبيعها).
  - كمية المخزون التلقائية.
  - **صورة المنتج (اختياري):** التاجر ليس ملزماً بوضع صورة، إن أراد يرفعها وإن لم يرد تظهر أيقونة رمزية جذابة.
  - **تاريخ انتهاء الصلاحية (Expiry Date):** حقل اختياري مع شارة تنبيه ملونة (تنتهي قريباً / منتهية الصلاحية).
  - **تاريخ الشراء والبيع (History Log):** تتبع أوتوماتيكي لدفعة المنتج (متى تم شراؤها ومتى تم بيع آخر قطعة).

### 3️⃣ عمليات البيع والشراء المترابطة (Interconnected Transactions)
عند فتح الشاشة الرئيسية، يجد التاجر زرين واضحين وكبيرين:

#### 🟢 زر "شراء جديد" (من مورد):
1. يختار المورد (أو يضيف مورد جديد بنقرة واحدة).
2. يختار المنتج والكمية وسعر الجملة.
3. يحدد المبلغ المدفوع كاش والمبلغ المتبقي كـ (دين للمورد).
4. **الربط التلقائي:** 
   - يزداد رصيد المخزون فوراً.
   - يتحدث تاريخ الشراء للدفعة.
   - يضاف الدين المتبقي في حساب المورد تلقائياً.

#### 🔵 زر "بيع جديد" (لزبون):
1. يختار الزبون (أو بيع سريع لكاشير بدون اسم).
2. يختار المنتج والكمية (يظهر سعر التجزئة تلقائياً ويمكن تعديله).
3. يحدد المدفوع كاش والمتبقي كـ (دين على الزبون).
4. **الربط التلقائي:**
   - ينقص المخزون أوتوماتيكياً.
   - يسجل تاريخ البيع.
   - يحسب الربح الصافي (سعر التجزئة - سعر الجملة).
   - يضاف الدين المتبقي على الزبون تلقائياً.

### 4️⃣ دفتر الديون الموحد والتصفية (Debt & Payments)
- **تبويبين واضحين فقط:**
  - 🔴 **ديون لي (على الزبائن):** قائمة بالزبائن الأكثر تأخراً مع زر "تأكيد سداد" بنقرة واحدة وزر "تذكير واتساب".
  - 🟠 **ديون عليّ (للموردين):** قائمة بالموردين والمستحقات القادمة لسدادها.
- **مشاركة الإيصالات وكشف الحساب:** 
  - إرسال نص مجهز فوري عبر واتساب أو فيسبوك / مسنجر بنقرة واحدة.
  - إمكانية تصدير/مشاركة كشف حساب أو فاتورة مبسطة بصيغة PDF تناسب الهاتف لتسليمها للزبون أو المورد مباشرة.

---

## 📐 4. مخطط قاعدة البيانات الذكي (Database Schema Outline)

```sql
-- 1. الأشخاص (الموردين والزبائن)
CREATE TABLE contacts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  phone TEXT,
  photo_url TEXT,
  type TEXT CHECK (type IN ('customer', 'supplier', 'both')) DEFAULT 'customer',
  notes TEXT,
  balance NUMERIC DEFAULT 0, -- موجب = دين لنا، سالب = دين علينا
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. المنتجات
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  barcode TEXT,
  photo_url TEXT,
  cost_price NUMERIC DEFAULT 0,     -- سعر الشراء بالجملة
  retail_price NUMERIC DEFAULT 0,   -- سعر البيع بالتجزئة
  stock_quantity NUMERIC DEFAULT 0,  -- الكمية بالحبة/القطعة
  min_stock_alert NUMERIC DEFAULT 5, -- حد التنبيه
  expiry_date DATE,                  -- تاريخ انتهاء الصلاحية (اختياري)
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 3. حركات الشراء والبيع (المبيعات والمشتريات)
CREATE TABLE transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tx_type TEXT CHECK (tx_type IN ('PURCHASE', 'SALE')) NOT NULL, -- شراء أو بيع
  contact_id UUID REFERENCES contacts(id) ON DELETE SET NULL,
  total_amount NUMERIC NOT NULL,
  paid_amount NUMERIC DEFAULT 0,
  debt_amount NUMERIC DEFAULT 0,
  status TEXT CHECK (status IN ('PAID', 'PARTIAL', 'DEBT')) DEFAULT 'PAID',
  notes TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 4. تفاصيل عناصر العملية (المنتجات والكميات والتواريخ)
CREATE TABLE transaction_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  transaction_id UUID REFERENCES transactions(id) ON DELETE CASCADE,
  product_id UUID REFERENCES products(id) ON DELETE SET NULL,
  quantity NUMERIC NOT NULL,
  unit_price NUMERIC NOT NULL, -- سعر الفردي بالعملية
  cost_price NUMERIC NOT NULL, -- سعر التكلفة وقت العملية (لحساب الأرباح)
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 5. سجل الديون والتحصيلات المباشرة
CREATE TABLE debt_payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  contact_id UUID REFERENCES contacts(id) ON DELETE CASCADE,
  amount NUMERIC NOT NULL,
  payment_type TEXT CHECK (payment_type IN ('COLLECTED', 'PAID_OUT')) NOT NULL, -- تحصيل دين من زبون / دفع دين لمورد
  note TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 🎨 5. معايير واجهة المستخدم (Mobile UX Standards for Simple Merchants)
1. **لا توجد تعقيدات أو قوائم متداخلة:** الشريط السفلي للهاتف يحتوي 4 أيقونات فقط:
   - 🏠 **الرئيسية:** زرين ضخمين (بيع 🔵 / شراء 🟢) + كروت ملخصة سريعة.
   - 📦 **المخزن:** قائمة المنتجات المحفوظة مع إشارة تاريخ الصلاحية.
   - 👥 **الجهات:** الموردين والزبائن مع أزرار الواتساب والاتصال.
   - 💳 **الديون:** سجل مَن له ومَن عليه مع التصفية السريعة.
2. **خطوط عربية عريضة وواضحة (Inter / Cairo Font).**
3. **دعم خاصيات الإدخال السريع بالصوت والباركود عبر كاميرا الهاتف.**
