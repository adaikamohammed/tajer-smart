# 📈 تحليل مشروع إدارة الديون (DebtManagerAR / منصة المحلات)

## 📌 1. نظرة عامة على المشروع
مشروع "ديوني" (DebtManagerAR) هو منصة تجارية متكاملة موجهة لأصحاب المحلات التجارية لإدارة الديون، العملاء، المشتريات، المبيعات، والمخزون مع إمكانية تحويله إلى نظام اشتراكات (SaaS).

---

## 🏗️ 2. الهيكلية التقنية (Tech Stack)
- **إطار العمل (Framework):** Next.js (App Router) + TypeScript.
- **مكتبات الواجهة والتنسيق:** Tailwind CSS + shadcn/ui.
- **قاعدة البيانات ومحرك الاستعلام:** Prisma ORM + PostgreSQL.
- **إدارة المستخدمين والصلاحيات:** نظام أدوار (Owner, Sub-Accounts, Super Admin) مع تشفير كلمة المرور وتحديد سعة الاستخدام.

---

## 🗄️ 3. تحليل نموذج البيانات الشامل (Prisma Data Model)

### أ. نموذج العملاء والموردين الموحد (`Customer`)
- **الحقول:** `id`, `name`, `phone`, `type` (`customer` / `supplier`), `customerTier` (`retail`, `wholesale`, `semi-wholesale`), `companyName`, `totalBalance`, `creditLimit` (سقف الدين للموردين/الزبائن), `publicId` (مُعرّف عمومي لمشاركة الرابط).
- **الذُكاء في التصميم:** استخدام جدول واحد يدمج الزبون والمورد مع تحديد النوع والمفاوضات المالية، مع إمكانية مشاركة رابط كشف حساب رقمي مبسط.

### ب. نموذج الديون والأقساط (`Debt` & `Installment`)
- **الحقول:** `amount`, `type`, `dueDate`, `notes`, `attachment` (صورة الإيصل/المنتج), `status` (`pending`, `paid`, `partial`), `priority`, `installmentTotal`, `installmentPaid`, `installmentAmount`.
- **الهدف:** تتبع دقيق للديون وسدادها سواء كدفعة واحدة أو مقسمة إلى أقساط ومواعيد استحقاق.

### ج. نموذج المنتجات والتسعير المتقدم (`Product`)
- **التسعير متعدد المستويات:**
  - `costPrice`: سعر الشراء / الجملة من المورد.
  - `sellingPrice`: سعر البيع بالتجزئة للزبون العادي.
  - `wholesalePrice`: سعر البيع بالجملة للزبائن الكبار.
  - `semiWholesalePrice`: سعر نصف الجملة.
- **الوحدات والعبوات:** `baseUnit` (قطعة)، `packUnit` (كرتون/صندوق)، `packMultiplier` (كم قطعة بالكرتون)، `packPrice` (سعر الكرتون الكامل).
- **تاريخ الصلاحية وتنبيهات المخزون:** `expiryDate` لتاريخ الانتهاء، `minStock` للحد الأدنى.

### د. نموذج فواتير البيع والشراء (`SaleInvoice` & `PurchaseOrder`)
- **فواتير المبيعات (`SaleInvoice`):** ترتبط بالعميل، وتسجل نوع الدفع (`CASH` كاش، `CREDIT` آجل/دين، `PARTIAL` جزئي)، مع إجمالي المبلغ والخصم والمدفوع.
- **أوامر الشراء (`PurchaseOrder`):** تسجل السلع المشتراة من المورد وسعر التكلفة الإجمالي وتحدث رصيد المخزون فوراً.

### هـ. نموذج الاشتراكات والمحلات (`User` & `SubscriptionEvent`)
- تحديد تاريخ نهاية الاشتراك `expirationDate` وحظر التعديل عند الانتهاء (Read-Only).
- تحديد الحد الأقصى للعملاء `maxCustomers` والتنبيه عند الوصول للحد.

---

## 💡 4. أهم المزايا الذكية المستفاد منها
1. **الربط المباشر مع الواتساب (WhatsApp Integration):** زر بضغطة واحدة بجانب كل زبون/مورد لفتح محادثة واتساب جنيسية تحتوي على تفاصيل الدين أو الطلب.
2. **رابط كشف الحساب الرقمي (Public Statement Link):** رابط فريد يُرسل للزبون ليطلع على كشف حسابه بدون الحاجة لتسجيل الدخول، مما يلغي الإحراج.
3. **التسعير المرن (سعر شراء، جملة، تجزئة، عبوة/كرتون):** مناسب جداً للتاجر الذي يشتري بالكرتون ويبيع بالقطعة أو التجزئة.
4. **دعم طابعات الفواتير الحرارية (80mm Thermal Printing):** طباعة إيصالات صغيرة تناسب طابعات البلوتوث المحمولة التي يحملها التاجر المتنقل في سيارته/شاحنته.
5. **إمكانية التصدير (Excel / PDF Export):** إتاحة حفظ نسخة احتياطية محلية للتاجر في أي وقت.

---

## ⚠️ 5. النواقص أو جوانب التعقيد التي يجب تفاديها للتاجر المتنقل
1. **الواجهات المعقدة وكثرة الجداول الفرعية:** نظام ديوني مصمم للمحلات الثابتة ذات الشاشات الكبيرة والموظفين المتعددين، مما يجعله معقداً جداً لتاجر متنقل يمسك الهاتف بيده أثناء القيادة أو التعامل مع الزبون في السوق.
2. **صعوبة التسجيل المباشر أثناء البيع السريع:** يتطلب خطوات كثيرة لإدخال الفاتورة الصريحة (إنشاء فاتورة -> اختيار عناصر -> تطبيق خصم -> اختيار طريقة الدفع).
3. **غياب ميزة الدفعات والوجبات (Batches Date Tracking):** تتبع تواريخ الشراء والبيع للمنتج الواحد غير مربوط بدقة في عملية البيع السريعة.
