# 📊 تحليل مشروع مخزن منصور (Alpha Storage & HR)

## 📌 1. نظرة عامة على المشروع
مشروع "مخزن منصور" هو نظام إداري تم تطويره لمركز تخزين وإدارة عمالة، يدمج بين **إدارة المخزون والتوريد** و**إدارة الموارد البشرية والحضور والغياب والسلفيات**.

---

## 🏗️ 2. الهيكلية التقنية (Tech Stack)
- **الإطار والواجهة (Frontend):** Next.js (App Router) + React.
- **التنسيق والواجهات:** Vanilla CSS بتصميم حديث (Glassmorphism & Dark Palette).
- **قاعدة البيانات (Backend & DB):** Supabase (PostgreSQL).
- **إدارة الجلسات والهوية:** Supabase Auth.

---

## 🗄️ 3. تحليل قاعدة البيانات والنمذجة (Database Schema)

### أ. جدول العمال (`workers`)
- **الحقول:** `id`, `name`, `phone`, `address`, `join_date`, `photo_url`, `daily_rate` (الأجر اليومي), `discount_rate` (معدل الخصم), `base_salary` (الراتب الأساسي).
- **الهدف:** متابعة المستحقات المالية لكل عامل وسجل بياناته الشخصية والصورة.

### ب. جدول الحضور والغياب (`attendance`)
- **الحقول:** `id`, `worker_id`, `date`, `status` (`present`, `absent`, `late`), `reason`, `note`, `discount_amount`.
- **الهدف:** تسجيل يوميات العمال مع حساب الخصم الآلي عند التأخير أو الغياب.

### ج. جدول أصناف المخزن (`inventory_categories`)
- **الحقول:** `id`, `main_type` (`carton`, `bottle`, `cap`, `material`), `sub_type`, `unit` (`piece` ..الخ), `min_stock_level` (حد التنبيه بالنقص).
- **الهدف:** تصنيف المنتجات والمواد المخزنة بدقة عالية مع وضع خطوط حمراء للتنبيه.

### د. جدول كميات المخزون (`inventory_stock`)
- **الحقول:** `id`, `category_id`, `quantity`, `updated_at`.
- **الهدف:** الاحتفاظ بالرصيد الحقيقي الحالي لكل صنف.

### هـ. سجل حركة المخزن (`inventory_transactions`)
- **الحقول:** `id`, `category_id`, `type` (`in` إدخال, `out` إخراج), `quantity`, `previous_quantity`, `new_quantity`, `note`, `created_at`.
- **الهدف:** تتبع تاريخي دقيق لكافة عمليات التوريد والسحب مع تسجيل الكمية السابقة والجديدة لمنع التلاعب.

### و. جدول السلفيات والمدفوعات (`worker_advances` & `worker_payments`)
- **الحقول:** `id`, `worker_id`, `amount`, `date`, `month` (YYYY-MM), `note`.
- **الهدف:** إدارة السلف والخصم من الراتب النهائي عند التصفية الشهرية.

---

## 💡 4. نقاط القوة والأفكار الذكية المستفاد منها
1. **تتبع حركة المخزن (Audit Trail):** تسجيل `previous_quantity` و `new_quantity` في كل عملية (إدخال/إخراج) يضمن عدم ضياع أي كمية ومعرفة تاريخ حركة المنتج.
2. **حد التنبيه بالنقص (`min_stock_level`):** ميزة ممتازة تظهر شارة تنبيه للتاجر عندما يقل المخزون عن حد معين لتذكيره بالشراء قبل النفاد.
3. **التصميم المباشر والواضح:** الواجهة تعتمد على كروت واضحة ومؤشرات ملونة تسلسية تتيح معرفة الحالة في ثوانٍ.
4. **تعدد الوحدات وتصنيف الأصناف:** تقسيم المنتج إلى صنف رئيسي وفرعي يسهل الفلترة في المحلات الكبيرة.

---

## ⚠️ 5. النواقص أو جوانب التعقيد التي يجب تفاديها للتاجر المتنقل
1. **التركيز على العمال والمصنع:** المشروع يحتوي على نظام حضور وغياب وعمالة مفصل، وهو غير مطلوب بحجمه الحالي للتاجر المتنقل الذي يعمل بمفرده أو مع مساعد بسيط.
2. **غياب ربط المشتريات بالديون والموردين:** في نظام منصور، عملية إدخال المخزون منفصلة تماماً عن الموردين وعن الديون المالية، وهذا عيب سنعالجه باقتدار في تطبيقنا (بحيث يؤدي الشراء أوتوماتيكياً لتحديث رصيد المورد وحساب الديون).
3. **عدم وجود تتبع لتواريخ انتهاء الصلاحية ومجموعات الشراء (Batches):** المنتجات تدخل بدون تاريخ شراء محدد لكل دفعة أو تاريخ انتهاء صلاحية.
